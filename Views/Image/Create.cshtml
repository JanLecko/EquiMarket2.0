@model EquiMarket.Models.Image

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Image</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.FilePath, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.FilePath)
                @Html.ValidationMessageFor(model => model.FilePath)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.HorseID, "HorseID", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("HorseID", String.Empty)
                @Html.ValidationMessageFor(model => model.HorseID)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
