@model DateTime?
@{
    var x = ViewData.ModelMetadata.DisplayFormatString;
}

@Html.TextBox("", Model.HasValue ? String.Format(ViewData.ModelMetadata.DisplayFormatString, Model.Value) : "", new { @class = "form-control", type = "date" })


