@model DateTime?

@{
    
    var x = ViewData.ModelMetadata.DisplayFormatString;
}

@Html.TextBox("", Model.HasValue ? Model.Value.ToShortDateString() : "", new { @class = "form-control", type = "date" })


